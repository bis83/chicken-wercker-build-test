box:
  id: lang/chicken:latest
  entrypoint: /bin/bash -c

build:
  steps:
    - script:
      name: chicken version
      code: csi -version 
    - script:
      name: chicken build
      code: csc main

